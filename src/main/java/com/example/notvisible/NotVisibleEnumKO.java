package com.example.notvisible;

/**
 * This enum should not appear in the schema, but it does because it's a property of NotVisibleResponse
 */
public enum NotVisibleEnumKO {
    VALUE1,
    VALUE2
}
