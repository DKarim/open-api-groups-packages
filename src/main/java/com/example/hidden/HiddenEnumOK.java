package com.example.hidden;

/**
 * This enum is properly hidden in the generated documentation because it's used in a response of a @Hidden controller it seems.
 */
public enum HiddenEnumOK {
    VALUE1,
    VALUE2
}
